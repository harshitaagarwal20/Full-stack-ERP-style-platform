{
  "name": "nimbasia-frontend-app",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend-hostinger install && npm --prefix frontend-hostinger run build",
    "start": "npm --prefix frontend-hostinger run preview -- --port 4173 --host"
  }
}
