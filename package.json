{
  "name": "nimbasia-frontend-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend-hostinger install --include=dev && npm --prefix frontend-hostinger run build && node scripts/copy-frontend-dist.js",
    "postinstall": "npm --prefix backend install --include=dev && npm --prefix frontend-hostinger install --include=dev && npm --prefix frontend-hostinger run build && node scripts/copy-frontend-dist.js",
    "start": "npm --prefix backend start"
  }
}
