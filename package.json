{
  "name": "nimbasia-frontend-app",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix frontend run preview -- --port 4173 --host"
  }
}
