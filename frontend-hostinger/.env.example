# API Base URL Configuration
# For local development: /api (relative URL, backend on same server)
# For network access: http://<your-machine-ip>:5000 (absolute URL)
# For production (same-origin, backend served by the same app): /api
VITE_API_URL=/api
