node_modules
.env
.DS_Store
dist
build
coverage
*.log
.prisma
backend/public

# Deployment build archives
*.zip

# Office lock files, created while a doc/template is open
~$*

# Database dumps (contain password hashes + customer PII — never commit)
*.sql
!backend/prisma/hostinger-init.sql
!backend/prisma/hostinger-additive-migration.sql
# Data-free repair script (schema-driven UPDATEs only, no dump content)
!backend/prisma/hostinger-fix-zero-dates.sql
